(ns urho.routes.error-routes
  (:require [compojure.api.sweet :refer :all]
            [compojure.route :refer [not-found]]
            [ring.util.http-response :as response]))

(defroutes error-routes
           )
